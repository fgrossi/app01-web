---
- hosts: dev 
  vars:
     ansible_python_interpreter: /root/anaconda3/bin/python
  become: True // perché ho bisogno del sudo per installare un package di cui ho bisogno
  tasks:
    - name: Start the container
      docker_container:
        name: app01-web
        image: francescogrossi/docker-ansible-demo:299894d       //${DOCKER_TAG}
        state: started
        recreate: yes
        detach: true
        ports:
          - "10080:8080"
